set from = "honza@betlach.name"
set realname = "Jan Betlach"

set imap_user = "honza@betlach.name"
set imap_pass = ""

set smtp_url = "smtp://$imap_user@smtp.1and1.com:587/"
set smtp_pass = $imap_pass

set folder = "{imap.1and1.com/ssl}"
set spoolfile = "+INBOX"
set record = "+Sent objects"
set postponed = "+Drafts"
set trash = "+Trash"

set move = no

mailboxes "+INBOX"

set header_cache = "~/.cache/mutt/headers" 
